# This directory doubles as the project-level Claude dir for the dotfiles repo,
# so Claude writes live state here. Allowlist config; ignore everything else.
*
!.gitignore
!settings.json
!plugins/
plugins/*
!plugins/installed_plugins.json
!plugins/known_marketplaces.json
