# Comments and blank lines are allowed.

RCS
.+,v

CVS
\.\#.+       # CVS conflict files / emacs lock files
\.cvsignore

\.svn
_darcs
\.hg

\.git
\.gitignore
\.gitmodules

.+~          # emacs backup files
\#.*\#       # emacs autosave files

^/README.*
^/LICENSE.*
^/COPYING

setup.sh
snapshot.sh
macos-defaults.sh
doctor.sh
Brewfile

# Repo infrastructure, not config — these belong to the repo, not to $HOME.
\.github
\.githooks
\.gitleaks\.toml

.DS_Store

# Agent config dirs hold live state alongside config, so stow must not fold them.
# setup.sh links the individual config files instead.
\.claude
\.codex
\.agents

# Never let stow near the ssh dir; setup.sh links config only.
\.ssh
